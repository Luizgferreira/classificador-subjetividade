from setuptools import setup, find_packages

setup(
    name="subcla",
    version='0.1',
    packages=['code', 'code.preprocessor', 'code.data',
              'code.manage', 'code.models.nb','code.models.smo','code.evaluation','code.models','code.data.model']
)
